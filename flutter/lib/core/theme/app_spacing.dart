/// Shared spacing scale. Use these instead of guessing SizedBox heights —
/// keeps every form in the app visually consistent without having to
/// eyeball each screen individually.
class AppSpacing {
  AppSpacing._();

  static const xs = 6.0;
  static const sm = 10.0;
  static const md = 14.0;
  static const lg = 20.0;
  static const xl = 28.0;
  static const headerMinHeight = 130.0;
}